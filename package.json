{
  "name": "full-stack-auth-app",
  "version": "1.0.0",
  "description": "Full-stack authentication application",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "db:setup": "npm run db:generate --prefix server && npm run db:push --prefix server",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "dev": "npx concurrently \"npm run dev:server\" \"npm run dev:client\""
  }
}
